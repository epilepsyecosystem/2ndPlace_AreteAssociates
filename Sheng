Bootstrap: docker
From: ubuntu:18.04

%labels
	CREATER Sheng
%post
	sudo apt -y update
	sudo apt -y install python3
	sudo apt install -y python3-pip
	pip3 install numpy scipy glob pandas sklearn json pdb csv

%files
	SETTINGS.json
	features.py /
	model.py / 
	
%runscript
	python3 /features.py
	python3 /model.py